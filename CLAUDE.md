# CLAUDE.md — KiroCrew

**`AGENTS.md` is the single source of truth for this repository.** Read it before
any non-trivial change; it is imported below so it is always in context. This
file intentionally holds no guidance of its own — do not add rules here, add them
to `AGENTS.md`. The frontend has its own `website/AGENTS.md`.

@AGENTS.md
