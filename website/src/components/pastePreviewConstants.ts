/** Lines shown in a paste hover preview before truncating with a "+N more" footer. */
export const PREVIEW_MAX_LINES = 12
/** Hover dwell (ms) before the preview opens — avoids flicker on cursor pass-through. */
export const PREVIEW_OPEN_DELAY_MS = 300
/** Max height of the preview panel (px); used for viewport flip positioning. */
export const PREVIEW_MAX_HEIGHT = 240
