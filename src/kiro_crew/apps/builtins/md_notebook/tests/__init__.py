"""Package marker so this app's tests are importable as a package.

Mirrors the other built-in apps (``auto_improvement/tests``, ``code_review_sage/tests``)
so two apps can each own a module of the same name without colliding on sys.path.
"""
